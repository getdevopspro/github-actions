name: All Green

on:
  workflow_call:

jobs:
  all-green:
    name: All Green
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v6

      - name: All Green
        uses: getdevopspro/github-actions/all-green@v7.1.0
