name: Release

on:
  push:
    branches:
      - cleanbotix
    paths-ignore:
      - "**.md"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  packages: write
  pull-requests: read
  statuses: read

jobs:
  release:
    uses: ./.github/workflows/release.yml
    secrets:
      checkout-token: ${{ secrets.BOT_REPO_TOKEN }}
    with:
      version-makefile: Makefile
      git-add-files: Makefile
      git-user-name: cleanbotix-cibot
      git-user-email: 286146176+cleanbotix-cibot@users.noreply.github.com
      make-target-pre-push: release-version
      changelog-enabled: true
      update-release-enabled: true
